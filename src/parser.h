/**
 * Copyright 2012 Daniel C. Rasband.
 */
#include "hangeul-romaja.h"
#include "types.h"

int hangeul_parse_str(Hangeul **hangeulRef, size_t size, const char *source);
int romaja_parse_str(Romaja **romajaRef, size_t size, const char *source);
